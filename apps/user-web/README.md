# Velura User Web

The current checkout/auth customer pages live under `src/pages`.

This folder is reserved for the long-term customer application boundary. The shared database schema already models user-created orders, reviews, return requests, and support tickets so admin and user flows can operate on the same data.
