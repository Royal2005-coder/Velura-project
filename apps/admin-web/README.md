# Velura Admin Web

The current admin UI lives under `src/pages/admin` and is served by the root Vite app.

This folder is reserved for the long-term admin application boundary. Migrate pages here only when the team is ready to split the current static UI into a dedicated app. Until then, keep UI changes in `src/pages/admin`, `src/scripts`, and `src/styles`.
